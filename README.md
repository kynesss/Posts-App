# Posts-App (Posts + Comments)

## Stack
- **Backend:** .NET (ASP.NET Core)
- **Database:** PostgreSQL (Docker)
- **Frontend:** React + Vite
- **Orchestration:** Docker Compose

## How to run (recommended — Docker)

Make sure Docker Desktop is running, then:

```bash
docker compose up --build
